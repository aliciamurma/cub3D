
Controls (Todo lo que se refiera a los controles del juego)
	onkey
Bitmap (Todo lo que se refiere a la crecion de imagenes)

Player (Lo que se refiere al movimiento del personaje)
	rotate (rota la vista del personaje)
	walk (mueve al personaje por el mapa)
	update (atualiza la posicion del personaje)
Map (Lo que se refiere al mapa)
	get (obtiene el mapa)
	cast (raycast)
		ray (no me queda claro como funciona)
		step (no me queda claro como funciona)
		inspect (no me queda claro como funciona)
	update (actualiza el mapa)
Camera (lo que se refiere a lo que se ve en la pantalla del juego)
	render (renderiza el mapa)
	drawSky (dibuja el cielo)
	drawColumns (dibuja las paredes)
	drawWeapon (dibuja el arma del pesonaje)
	drawColumn (dibuja cada columna)
	project ()
GameLoop (lo que se refiere al game loop o bucle del juego)
	start (inicia el juego)
	frame (informacion de cada frame)